{{- if .Values.clair.serviceAccount.create -}}
{{- if .Values.clair.rbac -}}
{{- if .Values.clair.psp -}}
apiVersion: policy/v1beta1
kind: PodSecurityPolicy
metadata:
  name: {{ printf "%s-clair-psp" (include "harbor.fullname" .) | quote}}
  labels:
{{ include "harbor.labels" . | indent 4 }}
spec:
{{ toYaml .Values.psp | indent 2 }}
{{- end }}
---
# Roles
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ printf "%s-clair-cr" (include "harbor.fullname" .) | quote}}
  labels:
{{ include "harbor.labels" . | indent 4 }}
rules:
{{ toYaml .Values.rbac | indent 2 -}}
{{- if .Values.clair.psp }}
  - apiGroups: ["extensions"]
    resources: ["podsecuritypolicies"]
    resourceNames: [{{ printf "%s-clair-psp" (include "harbor.fullname" .) | quote}}]
    verbs: ["use"]
{{- end }}
---
## Role bindings
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ printf "%s-clair-crb" (include "harbor.fullname" .) | quote}}
  labels:
{{ include "harbor.labels" . | indent 4 }}
subjects:
- kind: ServiceAccount
  name: {{ template "harbor.clair-serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ printf "%s-clair-cr" (include "harbor.fullname" .) | quote}}
{{- end -}}
{{- end -}}