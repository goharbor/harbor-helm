{{- if .Values.notary.enabled }}
{{- range $configMapName, $configMap := .Values.notary.configMaps }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $configMapName }}
data:
{{- range $dataKeyName, $dataValue := $configMap.data }}
  {{ $dataKeyName }}: {{ tpl (toYaml $dataValue) $ | indent 2}}
{{- end }}
---
{{- end }}
{{- end }}