{{- if and .Values.notary.enabled .Values.notary.server.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: "{{ template "harbor.notary-server" . }}"
spec:
{{- if .Values.notary.server.podDisruptionBudget.minAvailable }}
  minAvailable: {{ .Values.notary.server.podDisruptionBudget.minAvailable }}
{{- end }}
{{- if .Values.notary.server.podDisruptionBudget.maxUnavailable }}
  maxUnavailable: {{ .Values.notary.server.podDisruptionBudget.maxUnavailable }}
{{- end }}
  selector:
    matchLabels:
{{ include "harbor.matchLabels" . | indent 6 }}    
      component: notary-server
{{- end }}
